using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameOver : MonoBehaviour
{

    public int winScore = 0;
    //look at reset ball 
    // if scoreboard = 0 somebody won and pop up some text with "Left/Right Player won"
    //making it in a diff script so gotta make use of findobject
    //loadscene



    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {

    }
     
}
