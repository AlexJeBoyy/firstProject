using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Plan : MonoBehaviour
{
    /* making breakout.
     * 1. make the peddles, ball and bricks
     * 2. make scripts for the paddles (already mostly exists)
     * 3. make script for the ball
     * 4. script for the walls
     * 5. scripts for the bricks
     * make the bricks disapear when the ball hits it and get points from that.
     * make the ball go diferent dyrections depending on what side of the brick they hit
     */
}
